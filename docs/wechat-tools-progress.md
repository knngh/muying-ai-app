# 微信工具迭代进展（2026-09-21）

仅涉及微信小程序及必要后端。分支 `feature/tool-first-name-library`。本文记录代码与测试状态，不代表已发布或 15 套工具全部完成。

## 本轮产品调整

- 孕周与工具联动：保留日历周轴、原首页日历/档案卡片；“本周工具”可直接定位到“这周用得上”，随浏览周数切换 3 个入口和使用理由。工具打开后带有来源周数，返回时保留原浏览位置，不把浏览周数写入实际孕周或记录日期。
- 首页默认推荐、工具页“本周”标记与日历共用规则；用户自定义首页顺序仍保留。“恢复阶段推荐”会清除自定义偏好，之后继续跟随阶段变化。
- 视觉调整：15 项工具统一为细线图标，沿用橙/粉/绿/淡紫配色；首页常用工具在默认数量下呈三列，目录收起冗长的首页工具名单与逐行重复状态文字。海报继续只在工具栏中展示。
- 暂停知识库：首页、登录页、我的收藏入口隐藏；保留旧路由的暂停页面，不触发文章列表、详情或翻译请求。`mini-program/src/config/features.ts` 统一控制；网页与 App 不受影响。
- 首页“我的常用”：已知周数时，显示日历、3 项本周工具和海报；尚未完善阶段时采用基础推荐。用户可从工具目录或详情添加，最多 8 项。支持排序、移除、恢复推荐。明确保留空列表和自定义顺序。偏好保存在此设备。
- 工具目录：搜索名称/用途/常用同义词 + 固定四组（孕期记录、待产准备、宝宝照护、生活纪念），不再叠加快捷工具、记录摘要和大段介绍。每项单独提供打开及添加首页按钮。
- 按用户反馈恢复首页“孕周记录”“时光档案”原版大卡片、渐变和底部摘要。日历和档案页面保留原布局。
- 目录描述只展示已实现能力，进阶功能明确留在开发计划中。

### 工具出现的时机

| 浏览阶段 | 优先入口 |
| --- | --- |
| 孕 1—13 周 | 产检报告、孕期体重、孕育日记 |
| 孕 14—19 周 | 孕期体重、产检报告、孕育日记 |
| 孕 20—27 周 | 产检报告、待产包、宝宝起名 |
| 孕 28—36 周 | 胎动计数、待产包、孕期体重 |
| 孕 37—40 周 | 宫缩计时、胎动计数、待产包 |
| 出生后 1—4 周 | 喂养三件套、疫苗记录、孕育日记 |
| 出生后 5—12 周 | 喂养三件套、宝宝生长、成长相册 |
| 出生后 13—26 周 | 宝宝生长、疫苗记录、成长相册 |
| 出生后 27—52 周 | 辅食添加、宝宝生长、喂养三件套 |
| 出生后 53—156 周 | 辅食添加、宝宝生长、孕育记账 |

以上是工具发现时机，不是产检、胎动监测或辅食的医疗起始标准。文案提示按医嘱/门诊安排记录；所有 15 项工具始终可从目录主动打开。海报在首页保留，日历中不增加独立海报卡。工具记录汇总回写日历、跨账号全局记录隔离仍属于后续工作，本轮没有声称已完成。

## 当前 15 项工具与后续工作

| 工具 | 当前实现 | 后续重点 |
| --- | --- | --- |
| 孕育日历 | 原周轴、待办、周日记 | 工具日摘要、提醒资格与调度验收 |
| 宫缩计时 | 开始/结束、持久化起点、记录接口 | 实际会话间隔、恢复/时钟异常、就医准备待办 |
| 胎动计数 | 点按/撤销、保存会话 | 真实点按时刻与会话时长、断点恢复（当前不能视为完整计时器） |
| 孕期体重 | 录入、基础趋势、记录接口 | 基线/孕周/IOM 引用数据与适用条件 |
| 喂养三件套 | 喂养/尿布/睡眠基础记录 | 侧别、尿布类型、真实睡眠计时、今日摘要 |
| 宝宝生长 | 三项数值记录 | WHO LMS、生日/性别/测量方式匹配 |
| 待产包 | 基础单人勾选 | 审核后的完整模板、自定义、家庭认领与权限 |
| 疫苗时间表 | 实种/预约/待确认记录 | 逐剂规则与版本、国家免疫规划引用 |
| 辅食添加 | 食材/观察记录 | 食材去重、时间线、审核资料 |
| 产检报告 | 本机草稿、私有原图、分页、手动字段确认、删除 | OCR 与 Jev 候选整理、多页、日历事件关联 |
| 孕期海报 | Canvas 图片、预览、相册保存、固定入口小程序码接口 | 真机权限与扫码验收；需要服务端发布与有效微信配置 |
| 孕育日记 | 文字/心情、记录接口 | 图片、每日回看与模板周回顾 |
| 成长相册 | 本机照片记录入口 | 持久化照片、私有云资产与删除引用、月龄纪念卡 |
| 孕育记账 | 支出/分类、记录接口 | 退款/转账、月度汇总、Jev 一句话候选草稿 |
| 宝宝起名 | 静态样本库筛选/本机收藏 | 人工审核扩库、家庭讨论；不宣称万名库已完成 |

Jev 仍使用既有 TypeSafe 后端模块；本轮不向 Jev 发送真实报告、儿童图片或其他健康资料。报告自动识别尚未开放；手动原文与核对后值分开保存，不输出医学结论。

## 报告归档实现与部署约束

- `ReportDocument` / `ReportField` 为新增表。先在目标环境备份并同步 Prisma schema，再发布新后端；此轮只生成了 Prisma Client，没有操作现有数据库。
- 私有原图保存在 `private-uploads/<userId>/reports/`，不经过公开 `/uploads`，不使用公开 COS 直链。默认目录权限 0700、文件 0600。上线时需挂载持久卷、纳入受控备份，并确认反向代理不会公开该目录。
- 原图保持上传字节；解码校验格式、8MB/4000 万像素限制，仅支持单张 JPG/PNG/WebP。错误元数据、无效图片、重复上传和数据库失败会清理本次临时文件。
- 创建以 `(userId, clientOperationId)` 去重；用户/报告/字段三级作用域校验；确认字段须携带版本，旧版本返回 409。手动字段不能伪造 OCR 来源。
- 删除先撤销访问与删除字段，再删除原图；保留无名称/备注的空操作墓碑，阻止超时重试恢复已删档案。磁盘清理失败可以重复请求 DELETE，运维需重试清理失败记录。
- 本机草稿按游客与账号分开保存，图片使用持久化文件。游客草稿仅在用户点击导入后移入当前账号，不自动上传。不自动归属旧的全局工具草稿给任何账号。
- 云端列表采用 ID 游标分页；原图通过带 Authorization 的下载取得临时文件后预览；名称、图片、字段值不进入埋点。

API：`GET/POST /api/v1/tool-records/reports`，`DELETE /reports/:id`，`GET /reports/:id/file`，`POST /reports/:id/fields`，`POST /reports/:id/fields/:fieldId/confirm`。列表返回 `{list, nextCursor}`；创建正文要求 `reportDate/name/clientOperationId`，图片字段名 `file`，备注可选。

## 海报与微信配置

`GET /api/v1/tool-records/poster-code` 只生成固定 `pages/home/index` + `scene=beihu_poster` 的正式版小程序码，无姓名、账号或健康记录 ID。服务端使用 `WECHAT_APPID` 和 `WECHAT_APPSECRET`（兼容 `WECHAT_APP_SECRET`），合并并发请求、缓存一小时、失败退避一分钟。图片响应校验签名，微信错误信息不会透传凭据。没有有效配置时，纪念图片仍能生成，页面提示小程序码暂不可用。尚未对真实微信接口和二维码扫码做线上验证。

官方协议参考：
- https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/mp-access-token/getStableAccessToken.html
- https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/qrcode-link/qr-code/getUnlimitedQRCode.html

## 验证边界

孕周联动增量：12 项规则/首页逻辑测试通过，覆盖孕 1—40 周、产后 1—156 周范围与关键边界、无效来源参数、生日优先于旧孕周、API 时间戳格式的生日、恢复自动推荐。浏览器验收覆盖孕 12/20/28/37 周、出生后 27 周、来源提示、返回原周、实际孕周不变及 320/375/430 宽度布局；原有搜索/添加/排序/移除/知识库暂停链路回归通过。类型检查、微信构建及 ESLint 通过。本次模拟器自动化连接失败，未将其计作验证通过；仍需真机验收。

上一轮归档与导航检查：8 组相关测试共 43 项通过；后端 TypeScript 构建与 ESLint、小程序类型检查及微信构建通过。浏览器在 320/375/430 宽度完成布局和交互检查，已确认海报仅在工具栏出现、原日历和档案卡片保留。微信模拟器完成过首页与 15 项目录渲染检查；完整交互自动化遇到连接中断，不能作为全链路验收通过。

本轮增加有意义的接口/逻辑测试：未登录、跨用户读图、原图字节不变、路径约束、无效 ID/日期、上传失败清理、重复请求、删除墓碑、字段版本冲突与来源、列表分页、首页自定义/空列表/顺序、搜索同义词、报告账号隔离、小程序码缓存/错误响应。另修复不同工具相同服务端 ID 在本机相互覆盖的问题。

界面检查覆盖首页、目录、搜索、添加、排序、移除、重载保留、空搜索、用途分类和旧知识库路由。浏览器检查只用于布局与交互；微信文件、相册授权、相机与真实设备系统行为需微信开发者工具及真机继续验收。测试使用合成数据，不读取真实健康资料。

未执行数据库变更、线上部署或微信上传发布。既有全量测试涉及外部 AI 网关和旧测试文件的问题另行处理。
