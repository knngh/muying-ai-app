# P7 P0-P1 收口记录

更新时间：2026-05-05

## 1. 范围

本次完成 P7 的两个基础阶段：

- P0：API 契约收口
- P1：签到链路收口

## 2. P0 API 契约收口

已完成：

- 扫描小程序 / App 客户端 API 调用与后端 routes。
- 修复小程序会员 API 旧路径：
  - `/subscription/membership` -> `/subscription/status`
  - `/subscription/orders` -> `/payment/create-order`
- 修复小程序周报 API 旧路径：
  - `/subscription/weekly-report/latest` -> `/report/weekly/latest`
- 修复小程序套餐返回结构：
  - `plans` -> `list`
- 修复小程序周报 highlights 返回结构：
  - 对齐后端 `string[]`
- 对齐移动端签到 API 类型，补齐后端新增字段：
  - `consecutiveDays`
  - `streakDates`
  - `totalDays`
  - `checkedInToday`
  - `pointsEarned`

已确认无残留：

- `/subscription/membership`
- `/subscription/orders`
- `/subscription/weekly-report`
- `paymentParams`
- `planRes.plans`

## 3. P1 签到链路收口

已完成：

- 后端签到改为 Prisma transaction，确保签到记录、用户积分、积分流水同事务完成。
- 重复签到提前拦截。
- 并发重复签到通过唯一键错误兜底为 `今日已签到`。
- 签到返回补齐前端需要的兼容字段：
  - `consecutiveDays`
  - `streakDates`
  - `totalDays`
  - `checkedInToday`
  - `pointsEarned`
- 签到状态接口补齐：
  - `consecutiveDays`
  - `streakDates`
  - `totalDays`
- 小程序 profile 签到卡支持提交中状态、重复签到兜底和状态归一化。
- App 首页签到成功后即时写入新的签到状态字段。

## 4. 回归保护

新增：

- `tests/checkin-service.test.ts`

覆盖：

- 首次签到创建记录、增加积分、写积分流水。
- 重复签到不打开事务。
- 唯一键并发冲突映射为 `今日已签到`。
- 签到状态返回连续天数、总天数、月度签到和 streak dates。

已纳入：

- `npm run test:unit`

## 5. 验证记录

已通过：

```bash
npm run build
npm run test:unit
cd mini-program && npm run type-check
cd mobile && npx tsc --noEmit
```

## 6. 剩余边界

- 小程序会员页当前对齐的是后端建单能力，真实微信支付参数返回仍属于 P2 支付闭环范围。
- 支付回调真实验签切换仍按 `PAYMENT-CALLBACK-CUTOVER.md` 执行。
