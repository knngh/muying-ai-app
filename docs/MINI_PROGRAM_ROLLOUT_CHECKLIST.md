# 小程序上线前清单

更新时间：2026-04-03

## 已完成

- [x] 小程序 API 默认地址切换为 `https://beihu.me/api/v1`
- [x] 小程序 WebSocket 默认地址切换为 `wss://beihu.me`
- [x] 清理小程序源码中的裸 IP 与非加密 `ws://` 默认值
- [x] 小程序 `type-check` 通过
- [x] 小程序微信端构建 `build:mp-weixin` 通过
- [x] 后端 AI 并行实例已部署到 `3002`
- [x] `https://beihu.me/api/v1/ai/health` 已切到 `3002`
- [x] `https://beihu.me/api/v1/auth/me` 保持走主服务 `3000`
- [x] Nginx 已按路径分流：
- [x] `/api/v1/ai/*` -> `3002`
- [x] `/ws/ai` -> `3002`
- [x] 其它 `/api`、`/ws`、页面 -> `3000`
- [x] AI 健康检查已改为配置级检查，不再消耗上游模型 token

## 代码审查重点

- [x] AI 路由决策改为参考最近多轮用户消息，而不是只看最后一句
- [x] HTTP 链路中，育儿场景会同时检查 `question` 与 `context`
- [x] WebSocket 链路中，育儿场景会同时检查 `question` 与 `context`
- [x] 知识库重排已增加分数差保护，避免弱匹配结果抢占前排

## 需要你在开发者工具手动验证

- [ ] 微信登录成功后，首页可正常进入
- [ ] 首页进入“孕育时间轴”无域名错误
- [ ] 首页进入“AI答疑”无域名错误
- [ ] AI 首次提问能返回结果
- [ ] AI 多轮追问能延续上下文
- [ ] 未登录点击 AI 会正确引导到登录页
- [ ] 断网或超时时，AI 能展示错误提示而不是白屏

## 建议验收路径

1. 清理开发者工具缓存并重新编译
2. 用真实微信登录跑一遍首页、日历、AI 三条路径
3. 重点测试 3 类 AI 问题：
   - 孕期问题：如“孕早期出血怎么办”
   - 育儿问题：如“3个月宝宝夜醒频繁怎么办”
   - 多轮追问：先问症状，再追问“要去医院吗”
4. 若以上通过，再提交体验版
